name: .NET CICD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        working-directory: RepositoryAndUnitOfWorkPattern
        run: dotnet restore RepositoryAndUnitOfWork.csproj

      - name: Run tests
        working-directory: RepositoryAndUnitOfWorkPattern.Tests
        run: dotnet test RepositoryAndUnitOfWork.Tests.csproj --no-restore --verbosity normal